# Environment variables (contains ACCESS_TOKEN!)
.env

# VM Inventory (contains IPs and infrastructure details!)
vms.yml

# SSH Keys (NEVER commit these!)
ssh/
*.pem
*.key
id_*

# Runner data
runner-data/

# Logs
*.log
